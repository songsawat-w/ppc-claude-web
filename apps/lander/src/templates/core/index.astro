---
// ============================================================
// DEPRECATED — This file is kept for backward compatibility.
// All templates now use the Base Template Engine at:
//   apps/lander/src/templates/base/index.astro
// ============================================================

import BaseTemplate from '../base/index.astro';
import type { LPTemplateConfig } from '../../lib/template-types';

interface Props {
  config: LPTemplateConfig;
}

const { config } = Astro.props;
---

<BaseTemplate config={config} />
